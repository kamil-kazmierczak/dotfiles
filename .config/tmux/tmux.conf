set-option -a terminal-overrides ",alacritty:RGB"
# bind-key -r n run-shell "tmux neww open-file.sh"

bind -r h select-pane -L
bind -r j select-pane -D
bind -r k select-pane -U
bind -r l select-pane -D
